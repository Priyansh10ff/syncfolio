**What this changes**

**Why**

- [ ] `npm run build` passes locally
- [ ] `npm run lint` passes locally
- [ ] If this adds an automated suggestion source, it writes to
      `pending_updates`, not directly to the profile tables
- [ ] If this adds a new required env var, README + `.env.example` are updated
