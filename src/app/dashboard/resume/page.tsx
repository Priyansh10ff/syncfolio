export default function ResumePage() {
  return (
    <div className="max-w-xl">
      <h1 className="font-display text-2xl mb-2">Resume</h1>
      <p className="text-sm text-[var(--loom-muted)]">
        Generate a PDF resume from your profile data, with multiple templates.
      </p>
      <p className="text-xs text-[var(--loom-muted)] mt-4">Coming in Phase 3.</p>
    </div>
  );
}
